<h2>Tournament Administration</h2>
<button class="new-tournament">New Tournament</button>
<table>
	<tr><th>Game</th><th>Name</th></tr>
	{{#each this}}
		<tr><td>{{game}}</td><td>{{name}}</td></tr>
	{{/each}}
</table>